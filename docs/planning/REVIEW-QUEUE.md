# Review Queue

Durable cross-week state for the **Weekly Reviews** batch (⚪ Overhead, quota-exempt, ~5 SP, scheduled late-week).

**Last Updated**: 2026-09-12
**First run**: 2026-08-10 (WEEKLY G6, week of 2026-08-03) · design: [2026-08-10-g6-weekly-reviews-design.md](../superpowers/specs/2026-08-10-g6-weekly-reviews-design.md)
**Latest run**: 2026-09-12 (WEEKLY G21, week of 2026-08-24) — run 3, executing the recipe below; no new
methodology. **17 rows: 1 adopt · 10 defer · 1 pass · 3 propagate, plus 2 process rows** (3 / 1 / 2 / 11 by
slot). Two parks fired and one closed itself.

---

## How this works

This file **is** the methodology of record for the batch in this repository — there is no `docs/prompts/`
cadence doc here. It is also the **exclusion record**: the Reviewed log is what prevents re-reviewing the
same candidate week after week.

### The four slots

| #   | Slot                         | SP  | Direction    | What it does                                                                                                                        |
| --- | ---------------------------- | --- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Plugins (×2 candidates)      | 2   | Inbound      | Best not-yet-reviewed from the **official** Claude plugin store **and** best from the **wider internet**, each row tagged `source:` |
| 2   | Claude best-practices        | 1   | Inbound      | Top not-yet-reviewed candidate via date-aware web search                                                                            |
| 3   | Non-Claude AI best-practices | 1   | Inbound      | Same, sourced from non-Claude models/tools (Cursor, Codex, Copilot, Gemini CLI, …)                                                  |
| 4   | Cross-project propagation    | 1   | **Outbound** | **No web research.** Scans what shipped _here_ and decides what belongs _elsewhere_                                                 |

### Verdicts and routing sinks

| Slot | Vocabulary                       | Sinks                                                                                                                         |
| ---- | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 1–3  | `adopt` \| `defer` \| `pass`     | `adopt` → 🟤 BACKLOG entry · `defer` → Next-up park + re-trigger condition · `pass` → row only                                |
| 4    | `propagate` \| `defer` \| `pass` | `propagate` → `TODO.md` § 🔀 Spawned → _Cross-project propagation (out-of-tree)_ · `defer` → Next-up park · `pass` → row only |

The sink asymmetry is intentional: inbound `adopt` schedules work **here**, outbound `propagate` lands work
**elsewhere** (status user-maintained — this repo cannot verify out-of-tree completion). Do not homogenize them.

**Process rows** — a third row shape, outside both vocabularies. A run sometimes has to record a decision
_about the batch itself_ rather than about a candidate: whether a counter advanced, whether a park's condition
fired. These are written as Reviewed-log rows so the reasoning is not lost, but their Verdict cell states the
decision in its own words (e.g. **counter does NOT increment**) and they are **excluded from the
`adopt`/`defer`/`pass`/`propagate` tallies**. Count and report them separately — run 2 was "14 rows: 1 adopt ·
7 defer · 2 pass · 3 propagate, **plus 1 process row**". _(Documented 2026-08-15 on the PR #39 review: run 2
used the shape consistently and reported it correctly, but the vocabulary above did not admit it existed.)_

### The run recipe

1. Reconcile the batch's scope against its current definition in WEEKLY before starting.
2. Commit this file's skeleton for the run — the empty rows — **before** any research.
3. Run slots 1 → 4 **sequentially, in-session** (`WebSearch` / `WebFetch`). Append each row immediately after
   its research; commit per slot.
4. Route every verdict to its sink; park runners-up and defers under Next-up with re-trigger conditions.
5. **Re-check every count and attribution against the artifact it describes** — see below. Not optional, and
   not satisfied by having written the conventions down.
6. Close out: tick the WEEKLY checkboxes, flip its Summary-Table Status to `✅ PR #N`, update `docs/README.md`.

#### Step 5 in full — the re-check pass

Run 1 shipped four real review findings and **three shared one root cause**: a count or an attribution written
once and never re-read against the thing it describes. Conventions 9 and 10 state the rules; this step is what
makes them a control, because run 1 stated Convention 9's corollary and violated it one slot later. **A
convention a run can state and then not follow is not yet a control.**

Before close-out, assert each of these by re-reading the artifact — not from memory, and not from the note you
wrote when you first made the claim:

- [ ] **Counts match their source.** Every "N conventions", "N rows", "N adopts" claim equals what the file
      actually contains. The spec's conventions list and this file's must align 1:1, item for item — a
      convention added late must propagate back to the live spec.
- [ ] **Verdict tallies agree across all files** that state them (`REVIEW-QUEUE.md`, `WEEKLY.md`, `DONE.md`).
      Flipping one verdict invalidates every tally downstream of it.
      **Search for the contradiction, not for the confirmation.** Confirming that the right figure appears
      somewhere is not the check — the wrong figure can sit two hundred lines away in the same file and the
      grep still passes. Enumerate **every** statement of a value and compare them to each other; a value
      written into the run's _skeleton_ before its slot ran is the likeliest one to have been left behind.
      _(Added 2026-08-15 on the PR #39 review, which caught exactly this: run 2's park table still read
      `counter → run 2 of 2` from the pre-research skeleton while three later statements said the counter
      did not increment. The run's own check 2 grepped for the presence of "still 1 of 2", found it, and
      passed — a guard that could not fail.)_
- [ ] **Every "already present / already covered" claim quotes the matched line** (Convention 10). A `grep -l`
      hit is not evidence; read the line and paste it into the row.
- [ ] **Every `Origin` / attribution cell names things inside the window it claims** — the dates, PR numbers
      and task IDs must actually fall within the scan window recorded for that slot.
- [ ] **Cadence and "typical" descriptions match the run that just happened**, not the run that was imagined
      when the sentence was drafted.

### Standing lens

**Launch-push relevance, high adopt bar** (set 2026-08-10). Fit is judged against what helps ship the Mirox
storefront now — Next.js 14 / TypeScript / Prisma / Playwright / Vercel, solo developer, translation and
visual work. Expect **0–1 adopts** per run; borderline-but-promising → `defer` + park, not adopt. Anything
that only pays off post-launch defers by default. Re-scope this lens once the storefront has launched.

---

## Conventions

1. **Every slot writes a Reviewed-log row regardless of verdict**, `pass` included — one row per candidate
   considered, so four-to-five rows is the floor (one per slot), not the cadence: run 1 wrote **9**, of which
   slot 4's first-run memory sweep alone was 5.
   Run 2 wrote **14** (3 / 1 / 2 / 8 by slot) and run 3 wrote **17** (3 / 1 / 2 / 11 by slot, of which **2**
   are process rows), so the spread is real and still widening — a run's row count tracks how many candidates
   it _considered_, not how many slots it has. Slot 4 is what grows: 5 rows, then 8, then 11, because its
   window is everything shipped since the previous run and the runs keep drifting further apart.
   _(2026-08-10; sibling runs twice nearly dropped `pass` rows by composing the log from memory after the
   research. Cadence corrected on the PR #32 review — the original text read "four-to-five rows per run", a
   figure the very run that seeded it contradicted. Run 2's figure added 2026-08-15 by the step-5 re-check,
   which is the check that catches exactly this: a cadence sentence describing the run that was imagined
   rather than the runs that happened.)_
2. **Derive the "already in use" exclusion set from live `enabledPlugins`**, never from a hardcoded list —
   all three settings files: `.claude/settings.json`, `.claude/settings.local.json`, `~/.claude/settings.json`.
   _(2026-08-10; a sibling first-run retro found its hardcoded set had missed several enabled plugins.)_
3. **Verification gate** — every top pick must be confirmed against a **fetched primary source** with a real URL
   and date. Unverifiable ⇒ cannot be adopted. Where cheap, decide by _running_ the thing rather than reading
   about it. _(2026-08-10.)_
4. **Respect a park's re-trigger condition.** Do the cheap condition check and stop; do not re-read a
   condition-gated park's source. _(2026-08-10; that cost two sibling runs real time for nothing.)_
5. **One 🟤 per `adopt` is a minimum, not a cap.** Incidental process findings surfaced during a run route 🟤 by
   the source rule (Claude-surfaced), independent of any slot's verdict. What is forbidden is _manufacturing_
   entries out of `pass` / `defer` verdicts. _(2026-08-10.)_
6. **Sibling-project review history is prior evidence, not an exclusion list.** Skip a candidate only when its
   rejection reason elsewhere was _universal_ (Python-only tooling; Claude Code ships it built-in); re-evaluate
   any whose reason was _project-specific_. Record the prior in the row. _(2026-08-10.)_
7. **Deferred: the three-scope lens.** The universal-config repo weighs each inbound candidate for fit _here_,
   _globally_ (`~/.claude`), and _for other active projects_, letting a `pass` still route outward. Not carried
   here. **Re-trigger**: a second active project starts consuming this repo's conventions, or a run produces a
   candidate with an obvious non-local fit and no sink for it. _(2026-08-10.)_
8. **No parallel subagent fan-out for web research.** Sequential in-session only — fan-out is a rate-limit
   hazard and gets OOM-killed in this devcontainer. _(2026-08-10.)_
9. **Slot 4 must scan the memory files, not just the PRs.** Auto-memory lives in
   `~/.claude/projects/<project>/memory/` and is **per-project by construction** — a durable process rule
   captured only there reaches no other project by any route. That is precisely where both of run 1's
   `propagate` verdicts were found, while the conventions that had reached a doc or template turned out to
   have already gone global. Corollary for testing an existing propagation: grep the **specific changed
   strings** in the live `~/.claude` tree, excluding `projects/` (per-project memory) and `plugins/`
   (third-party caches) — never diff the trees, which are scrubbed and drifted by design. _(2026-08-10.)_
10. **An "already present" claim requires reading the matched line, not the filename.** `grep -l` for a
    _concept_ proves only that some line matched some pattern. Run 1 shipped a wrong `pass` this way: a
    concept-grep for `bidirection\|both directions` matched `- [ ] Migration tested both directions` — a
    database-migration item — and the verdict asserted a docs-index rule was already global. Quote the
    matched line into the row, or the "already covered" half of a `pass` is unverified.
    _(2026-08-10, from the PR #32 review; this is the failure mode Convention 9's corollary exists to
    prevent, so the corollary was right and the run did not follow it.)_

---

## Reviewed log

**Run 3 windows (declared 2026-09-12, before any research).** Slots 1–3: candidates not already
present in this log, judged under the standing launch-push lens. Slot 4: everything shipped here
since run 2's merge `85caf2b` — PRs **#40, #41, #42, #43, #44, #45, #46**, the bare merges
`745e039` (G11), `9fc4fd3` (G12) and `b836e77` (G15), plus every auto-memory file created or
materially updated **2026-08-17 → 2026-09-12**. This is wider than the window WEEKLY named when the
plan was written on 2026-08-20 ("PRs #39/#40, G11 merge `745e039`, G12 merge `9fc4fd3`, memory files
2026-08-15 → run day") — G16–G20 had not shipped yet. The slot's own definition ("shipped since run
2") governs, widened by user ruling at the G21 brainstorm, exactly as run 2 was widened at G10's.
The rows below were committed as empty placeholders **before** any research (recipe step 2, commit
`527b612`); every cell was then filled from the artifact it describes rather than from the skeleton — which is
the failure run 2 shipped, where a counter written into the skeleton outlived the slot meant to set it.

### 1. Plugins

| Date       | Run     | Candidate                                                                                                                                   | `source:`            | Primary source                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Verdict                                                                       | Reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Routed to                                                                 |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| 2026-08-10 | 1 (G6)  | `resend` — Resend agent skills + MCP                                                                                                        | official             | [resend/resend-skills](https://github.com/resend/resend-skills) @ `7fa08a1`, MIT; manifest fetched 2026-08-10                                                                                                                                                                                                                                                                                                                                                      | **defer**                                                                     | Maps to the immediate next group (G5 transactional emails), but its weight sits in `react-email` — a rewrite of the hand-rolled HTML-string templates, outside G5's copy/styling scope — and a hosted MCP needing OAuth. Read the actual `email-best-practices` SKILL.md rather than the blurb: it reduces to double opt-in (already implemented here) plus DNS authentication, whose provisioning half is already covered by the [2026-08-07] 🔵 "Verify prod email config" entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Next-up park; 1 incidental 🟤 (`lang` attribute)                          |
| 2026-08-10 | 1 (G6)  | `nextjs-marketplace` — Next.js 14+ App Router plugin set (10 plugins incl. "i18n & Localization Pro")                                       | internet             | [rdimascio/nextjs-marketplace](https://github.com/rdimascio/nextjs-marketplace), fetched 2026-08-10                                                                                                                                                                                                                                                                                                                                                                | **pass**                                                                      | Nominally an exact fit (Next.js 14 App Router, and i18n is the likely next-week spine), but it fails the provenance bar: **1 star**, single maintainer, and installation is `cp -r plugins/* .claude/plugins/` rather than a marketplace add. Surveyed alternatives fail differently — `claude-mem` duplicates the installed `auto-memory`, `frontend-design`/`playwright`/a11y tooling are already installed, and the i18n "skills" on aggregator sites have no traceable source repo (mcpmarket.com also returned HTTP 429, so it could not be verified at all). Unverifiable ⇒ not adoptable (Convention 3).                                                                                                                                                                                                                                                                                                                                                                          | 1 incidental 🟤 (next-intl `useExtracted`)                                |
| 2026-08-15 | 2 (G10) | **`claude-security`** — in-session deep vulnerability scan (7 agents: inventory → researcher → verifier → patch-generator → patch-verifier) | official             | Local marketplace clone `anthropics/claude-plugins-official`, refreshed 2026-08-15T13:43Z; `plugin.json` **v0.10.0**, author Anthropic; [homepage](https://github.com/anthropics/claude-plugins-official/tree/main/plugins/claude-security). README + agent/hook/skill tree read directly, not the store blurb.                                                                                                                                                    | **adopt**                                                                     | The park run 1 opened ("pre-launch security pass is scheduled, or real customer traffic is imminent") is **fired**: the pre-launch week is user-confirmed for **Aug 17–21 — this Monday** — and already inherits the G2 hardening bundle pinned to "before real customer traffic". Decided by reading the plugin, not the blurb: findings reach the report only after independent verifiers try to _disprove_ them, and the verification tally is computed in code rather than asserted by the model that produced the findings — which is the exact failure mode this repo has hit 18 times as [code-review-threshold-understates-doc-findings]. Preconditions **measured, not assumed**: Python **3.11.2** ≥ the required 3.9 ✅, git checkout ✅, not in the 16-plugin in-use set ✅. Scope fits the surface that actually ships: auth, API routes, order creation, admin panel, HMAC unsubscribe tokens, guest COD checkout. On-demand — no per-turn cost, unlike its sibling below. | 🟤 BACKLOG [2026-08-15] (the adopt) — run the scan in the pre-launch week |
| 2026-08-15 | 2 (G10) | `security-guidance` — always-on hooks: regex warnings on Edit/Write, LLM diff review on Stop, agentic commit reviewer                       | official (runner-up) | Same clone; `plugin.json` **v2.0.7**, author Anthropic (David Dworken); README read in full.                                                                                                                                                                                                                                                                                                                                                                       | **defer**                                                                     | Same fired park, and genuinely good — but the wrong shape for this week. Its value is a **per-turn tax**: an LLM call (`SECURITY_REVIEW_MODEL=claude-opus-4-7` by default) on every Stop plus an agentic reviewer on every commit, against a week already deliberately overloaded at 2× capacity. It secures code _as it is written_, so its payoff is spread across future development; `claude-security` concentrates its payoff at the launch gate, which is the decision this week actually faces. Now **reviewed** rather than parked-unreviewed — the run 1 park is closed and replaced with a narrower condition.                                                                                                                                                                                                                                                                                                                                                                 | Next-up park (re-trigger rewritten)                                       |
| 2026-08-15 | 2 (G10) | `logic-lens` — logic-first review via explicit execution tracing (Premises → Trace → Divergence → Remedy), 6 skills, 9 risk categories      | internet             | [hyhmrright/logic-lens](https://github.com/hyhmrright/logic-lens), fetched 2026-08-15: **19 stars**, MIT, v0.6.10, installs as a real marketplace add (`/plugin marketplace add hyhmrright/logic-lens`)                                                                                                                                                                                                                                                            | **defer**                                                                     | Clears the provenance bar run 1's `nextjs-marketplace` failed on — MIT licence and a genuine marketplace add rather than `cp -r plugins/*` — and it aims squarely at this repo's best-documented review weakness: a reviewer that reports only findings carrying a traced divergence chain is the complement to the ≥80 threshold that has suppressed real findings 18 times here. Held back by two things, so borderline-but-promising → `defer` per the standing lens, not adopt: **19 stars / single apparent maintainer**, and its 78.3% benchmark is **self-published**, not independent. Adopting a _second_ reviewer mid-launch-push also adds review rounds exactly when the week has none to spare.                                                                                                                                                                                                                                                                             | Next-up park                                                              |
| 2026-09-12 | 3 (G21) | `security-guidance` — fired park re-check (run 2 `defer`)                                                                                   | official             | Live `~/.claude/settings.json` read 2026-09-12: `"security-guidance@claude-plugins-official": true`. The plugin cache now holds **v2.0.8** alongside the v2.0.7 run 2 reviewed.                                                                                                                                                                                                                                                                                    | **not a candidate — it is in use** _(process row, excluded from the tallies)_ | Convention 2 derives the exclusion set from live `enabledPlugins`, and this plugin is enabled **globally**, so it reaches this project too. Run 2 deferred it on per-turn cost; it was adopted by another route since, which settles the park more firmly than a verdict could. No re-review was performed and its README was not re-read — there is nothing left to decide.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Next-up: the run 2 park is **closed**.                                    |
| 2026-09-12 | 3 (G21) | `logic-lens` — fired park re-review (run 2 `defer`)                                                                                         | internet             | [hyhmrright/logic-lens](https://github.com/hyhmrright/logic-lens) fetched 2026-09-12: **22 stars** (19 at run 2), **v0.6.10 — unchanged**, MIT, 118 commits. Benchmarks still internal (104 cases; self-reported 53.9% → 76.2% → 78.3% version-over-version on a 36-case subset), citing arXiv 2603.01896 for structured-reasoning accuracy — third-party _research_, not third-party validation of logic-lens itself.                                             | `defer`                                                                       | The condition fired on the **problem** side, not the candidate side. The threshold counter is at **24**, and G17 put scores on the record for the first time (6×75 + 1×65 against an 80 gate, all seven real), so the recurrence is better evidenced than ever. The candidate is exactly where run 2 left it: same version, +3 stars, still a single maintainer publishing his own benchmark. Meanwhile the park re-check surfaced the mechanism the BACKLOG already names — the rubric emits only 0 / 25 / 50 / 75 / 100, so an 80 gate is in practice a 100 gate. **A second reviewer does not fix a quantization defect in the first one**; it pays review rounds to route around it. Re-parked under a narrowed condition.                                                                                                                                                                                                                                                           | Next-up (narrowed) + a fold-in on the live 🟤 rubric entry.               |
| 2026-09-12 | 3 (G21) | **`neon`** — neon-postgres agent skill + the Neon MCP Server                                                                                | official             | Live official manifest (clone refreshed 2026-09-12): entry `neon` is a `git-subdir` of [neondatabase/agent-skills](https://github.com/neondatabase/agent-skills) at `2e0da3a`. `plugin.json` fetched 2026-09-12 — **v1.2.0**, author Neon, **Apache-2.0**; `mcp.json` ships exactly one HTTP server, `https://mcp.neon.tech/mcp`. Capabilities and warnings from [neon.com/docs/ai/neon-mcp-server](https://neon.com/docs/ai/neon-mcp-server), fetched 2026-09-12. | `defer`                                                                       | Chosen over `sentry` and `prisma` because production here **is** Neon, and the repo has a concrete incident this would have diagnosed in seconds — the PR #22 prod schema drift, where unmigrated `reviews` / `subscribers` tables 500-ed the homepage. It still does not clear the bar, because the vendor excludes the use case that motivated it: _"We recommend MCP for **development and testing only**, not production environments"_ and _"Never connect MCP agents to production databases"_. The narrowing knobs reduce the blast radius without answering the objection — `?readonly=true` limits to SELECT plus schema inspection and `?projectId=` scopes to one project, but a read-only prod connection still exposes **customer PII** (order addresses, phone numbers), and G18 shipped six days ago for the express purpose of not disclosing that. Local development runs Docker Postgres, not Neon, so the dev-and-test mode the vendor does endorse has no host here. | Next-up park, schema-only re-trigger.                                     |

### 2. Claude best-practices

| Date       | Run     | Candidate                                                                                                                                           | Primary source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Verdict   | Reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Routed to                                                                              |
| ---------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- |
| 2026-08-10 | 1 (G6)  | **Shrink CLAUDE.md: `/doctor` trim check + path-scoped `.claude/rules/*.md`** (`paths:` frontmatter)                                                | [code.claude.com/docs/en/memory](https://code.claude.com/docs/en/memory), fetched 2026-08-10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **adopt** | Decided on measurement, not on the write-up. This repo's `CLAUDE.md` is **350 lines** against a first-party "target under 200 lines… longer files reduce adherence", and **232 of those 350 (66%)** sit in Architecture / Detected Patterns / Git Insights — precisely the derivable content `/doctor`'s trim check is documented to cut. `.claude/rules/` does not exist here yet. Both preconditions verified against the installed **CC 2.1.226**: the invalid-`[`-pattern bug (Read failing for every evaluated file) was fixed in 2.1.207, and the brace-expansion startup crash in 2.1.217. Translates into a committed repo artifact, so it can actually be filed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 🟤 BACKLOG [2026-08-10] (the adopt)                                                    |
| 2026-08-15 | 2 (G10) | **"The new rules of context engineering for Claude 5 generation models"** — unhobble, judgement-over-rules, progressive disclosure, rich references | [claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models), published **2026-07-24**, fetched 2026-08-15                                                                                                                                                                                                                                                                                                                                                                                       | **defer** | First-party and current (this session runs Opus 5, so it is on-model). Checked rule by rule against what is already here, quoting each match rather than grepping for the concept (Convention 10). **Already in flight** — rules 1 & 4 (shrink, progressive disclosure) are the still-open run-1 adopt: _"**232 of those 350 lines (66%)** are the Architecture tree, Detected Patterns and Git Insights sections — exactly the derivable content `/doctor`'s trim check is documented to cut"_. **Already consumed** — rule 6 (lean on auto-memory): `.claude/settings.local.json` carries `"auto-memory@severity1-marketplace": true`. **Already practiced** — rule 7 (rich references over markdown specs): the Mirox design handoff is 7 `.dc.html` artifacts that G14 audited shipped pages against directly, not prose mockup descriptions. **Genuinely new**: rule 2, _"Write code that reads like the surrounding code"_ over rigid counts — a **qualitative** rewrite of instruction files, orthogonal to the run-1 entry's line-count argument, which no open item covers. Deferred rather than adopted because that rewrite is Cleanup-Week-shaped and Cleanup Week is user-confirmed for **after launch** — the lens's own "anything that only pays off post-launch defers by default". Its highest-value target is the **global** `~/.claude/CLAUDE.md` (rigid "minimum 3 alternatives / minimum 5 edge cases / every 50 lines" counts), which this repo has no sink for — see the Convention 7 note below. | Next-up park + amends the standing run-1 🟤 (new axis, not a new entry — Convention 5) |
| 2026-09-12 | 3 (G21) | **"How and when to use subagents in Claude Code"** — when delegation pays, when it does not, and the fresh-context verification pattern             | [claude.com/blog/subagents-in-claude-code](https://claude.com/blog/subagents-in-claude-code), published **2026-04-07**, fetched 2026-09-12. Date-aware search over `anthropic.com` / `claude.com` / `code.claude.com` surfaced **nothing first-party published since run 2's 2026-08-15 cutoff**; the field was evergreen posts, of which the two runners-up were "Using Claude Code: session management and 1M context" (2026-04-15) and "How Claude Code works in large codebases" (2026-05-14 — enterprise-scale, DRI roles and dev-ex teams, and its lean-CLAUDE.md advice is already the standing 🟤 from run 1). | `adopt`   | It supplies the number **Convention 8 is missing**, and this run found that convention overstating its own evidence. Convention 8 reads "fan-out is a rate-limit hazard and gets OOM-killed in this devcontainer" — but the repo has since measured both regimes and they diverge. Background **Workflow** fan-out at width really did OOM: TASK-038b, 2026-07-17, the workflow died 3×, `oom_kill 1` at an **8.45 GiB** peak against the WSL2 VM's 9.7 GiB with no cgroup cap. Bounded **foreground** dispatch did not: G17 ran **38 agents** at a **+550 MiB** peak with `oom_kill 0`, and even the run that was killed by session limits peaked at 4.86 GiB of 9.71 with `oom_kill 0`. So the convention is right about the hazard and wrong about its scope, and it currently forbids a shape the repo has measured as safe. The post supplies the trigger the amendment needs — _"When a task requires exploring ten or more files, or involves three or more independent pieces of work, that's a strong signal to direct Claude toward subagents"_ — plus the cost caveat that keeps the bar high: _"subagents carry overhead. Each one spins up its own context, consumes tokens, and adds a layer of indirection"_.                                                                                                                                                                                                                                                                                             | 🟤 BACKLOG [2026-09-12], scoped to amend Convention 8 only.                            |

### 3. Non-Claude AI best-practices

| Date       | Run     | Candidate                                                                                                                                                                               | Primary source                                                                                                                                                                                                                                                                                                                                                                                                                             | Verdict                                                                                 | Reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Routed to                                                                                               |
| ---------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 2026-08-10 | 1 (G6)  | Cross-tool agent practices — shared-context instruction files (`AGENTS.md` / `CODEX.md` / `.cursorrules`), multi-agent MCP memory, usage-budget monitoring                              | Survey of 2026 comparison write-ups (Cursor / Copilot coding agent / Codex / Antigravity); all aggregator-tier, no first-party source cleared the bar                                                                                                                                                                                                                                                                                      | **pass**                                                                                | Every substantive practice is already satisfied, already consumed, or structurally inapplicable. Per-project instruction files: `CLAUDE.md` exists. Shared MCP memory across agents: the `memory` MCP server is already enabled here. Rules-file scoping (Cursor `globs` / Copilot `applyTo`): this is exactly what slot 2 adopted natively as `.claude/rules` `paths:` — recording it here too would double-file one finding. `AGENTS.md` and multi-agent shared context are gated on a **second agentic tool**, which a solo single-tool project does not have; that park's condition is unmet, so it got the cheap check and no re-read (Convention 4).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Next-up (category-bias watch only)                                                                      |
| 2026-08-15 | 2 (G10) | **GitHub `spec-kit` / `specify` CLI** — spec-driven development as a cross-agent toolchain (`constitution` → `specify` → `plan` → `tasks` → `implement`, plus `analyze` and `converge`) | [github/spec-kit](https://github.com/github/spec-kit), fetched 2026-08-15: **128.9k stars**, MIT, GitHub org, 30+ agent integrations. Companion academic source [arXiv 2606.27045](https://arxiv.org/pdf/2606.27045) ("Spec Growth Engine", Grabowski, 2026-06-26) was fetched but its **body would not extract** from the PDF object stream — recorded as considered-and-unverifiable, and **nothing is claimed from it** (Convention 3). | **pass**                                                                                | The pipeline itself is already run here, by a different toolchain: superpowers' brainstorming → design spec in `docs/superpowers/specs/` → writing-plans → implementation plan → TDD → close-out maps onto `specify`/`plan`/`tasks`/`implement` step for step, and `constitution` onto `CLAUDE.md` + `PROJECT.md`. Installing a second SDD toolchain would duplicate it. **But two of its steps are the ones this repo performs by hand, repeatedly**: `/speckit.analyze` ("Cross-artifact consistency & coverage analysis") and `/speckit.converge` ("Assess the codebase against spec/plan/tasks and append remaining work as new tasks"). The first is precisely what **G11**, the OVERDUE docs-freshness linter scheduled this same week, is being built to do — the drift pair has been human-caught on PRs #16, #17, #19, #21, #23, #26 and #33. The second is what **G14's design-gap audit did manually** on 2026-08-15: compare 7 handoff files against shipped pages and file the deltas. Passed as a tool, routed as a **design input** to the linter that is already scheduled.                                                                                                                         | Design input → 🟤 [2026-08-01] From: PR #26 review — the docs-freshness entry (amended, not duplicated) |
| 2026-08-15 | 2 (G10) | **Bias-watch counter — does run 2 increment it?**                                                                                                                                       | Run 1's own recorded reason (this file, slot 3 row 1)                                                                                                                                                                                                                                                                                                                                                                                      | **counter does NOT increment**                                                          | The condition is narrow and worth reading precisely: file the 🟤 only "if slot 3 passes for **this same reason** in two more runs". Run 1's reason was structural — a _single-tool, solo_ project cannot adopt **cross-tool portability** practices (`AGENTS.md`, shared MCP memory). Run 2 passed for an entirely different reason: the candidate is **not portability-shaped at all**, it is a methodology this repo already runs under another name, and it still yielded a live design input for a group scheduled this week. So the category is **not** producing a foregone conclusion — it produced a usable finding as soon as the search stopped aiming at portability. The park is rewritten to name the real failure mode (searching the category for portability practices) rather than the category itself — and because the condition changed, its **counter re-bases to 0 of 2**: run 1's portability-aimed pass no longer qualifies under the new rule, and run 2 yielded a usable finding.                                                                                                                                                                                                         | Next-up park (condition rewritten; counter re-bases to 0 of 2)                                          |
| 2026-09-12 | 3 (G21) | **EARS acceptance-criteria notation** as used by Kiro's spec-driven flow (`requirements.md` / `design.md` / tasks, requirements written `WHEN … THE SYSTEM SHALL …`)                    | [kiro.dev/docs/specs/feature-specs](https://kiro.dev/docs/specs/feature-specs/), page **updated 2026-08-04**, fetched 2026-09-12. EARS itself is vendor-neutral — Alistair Mavin and colleagues at Rolls-Royce, presented at IEEE RE09 (2009), for aircraft-engine airworthiness requirements; Kiro (AWS) adopted it rather than inventing it.                                                                                             | `defer`                                                                                 | Searched for methodology, not portability, per the rewritten condition — and the candidate is squarely methodological: a syntax that forces every requirement into a testable shape, _"WHEN a user submits a form with invalid data THE SYSTEM SHALL display validation errors next to the relevant fields"_. It lands on the half this repo already does, and misses the half that keeps failing. The authoring half is independently derived here: the PR #18 refinement states that the review gate _"suppresses findings with no checkable referent. Docs that declare their own acceptance criteria are reviewable like code"_, and G19 shipped guards written that way. The verification half is where the recurrences live — a guard that cannot fail looks exactly like one that passes — and Kiro's own documentation does **not** cover it: it claims requirements _"can be directly translated into test cases"_ and offers _"traceability"_, with no step that proves a criterion can be violated. Adopting a notation whose gap is the repo's actual weakness buys formality, not coverage — and the launch push cannot spend a spec-format migration on it. The usable part routes as a design input. | Next-up park + design-input note on the live 🟤 rubric entry.                                           |
| 2026-09-12 | 3 (G21) | **Bias-watch counter — does run 3 increment it?**                                                                                                                                       | This file, the run 1 park as rewritten on 2026-08-15                                                                                                                                                                                                                                                                                                                                                                                       | **counter does NOT increment; stays 0 of 2** _(process row, excluded from the tallies)_ | The rewritten condition counts only runs where a methodology-aimed search _yields nothing usable_. Run 3's search was aimed correctly — EARS is a methodology, not an `AGENTS.md`-shaped portability artifact, so the search the park warned about was not repeated — and it yielded a design input that routes to a live entry. A `defer` with a usable output is not a barren slot. The counter therefore stays at **0 of 2**, its third consecutive run without advancing, which is itself worth noting: under the rewritten condition the slot has never once come up empty.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Next-up: park re-parked unchanged, counter 0 of 2.                                                      |

### 4. Cross-project propagation

| Date       | Run     | Learning scanned                                                                                                                                                                                                                                                                                                                                                                 | Origin                                                                                                                                                                                                                                                                         | Verdict       | Reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Routed to                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| 2026-08-10 | 1 (G6)  | **Visual-fidelity gate for design tasks**                                                                                                                                                                                                                                                                                                                                        | TASK-035 → PR #23 (origin); reinforced TASK-057 / TASK-036 / TASK-037 / G1 / G4                                                                                                                                                                                                | **propagate** | Verified absent from the whole global tree (`CLAUDE.md`, `WORKFLOW.md`, `POLICIES/*`, `TEMPLATES/*`) — grepped for screenshot / rendered-page / design-handoff concepts, hits only in plugin caches and per-project memory. Applies to any project with a UI; caught real defects in four-plus tasks here. Natural home: `POLICIES/manual-testing.md`. **Origin corrected on the PR #32 review** — the cell first read "PRs #24–#31", which both misdates the gate (TASK-035 shipped the failure; PR #23 made the sign-off standing) and overruns this slot's own #28–#31 + #27 scan window.                                                                                                                                                                                                                                                        | `TODO.md` § 🔀 Spawned → Cross-project (row 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-10 | 1 (G6)  | **"Never write execution records ahead of execution"**                                                                                                                                                                                                                                                                                                                           | G4 self-review (PR #31)                                                                                                                                                                                                                                                        | **propagate** | Verified absent globally. Failure mode is universal, not project-shaped: a plan drafted with pre-checked boxes and invented SHAs / PR numbers before the work happened. One-line addition to `POLICIES/documentation.md`, pairing with the existing read-SHAs-from-`git rev-parse` rule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `TODO.md` § 🔀 Spawned → Cross-project (row 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-10 | 1 (G6)  | **String renames must sweep every locator type**                                                                                                                                                                                                                                                                                                                                 | G4 / PR #31 CI failure                                                                                                                                                                                                                                                         | **defer**     | Genuine and evidence-backed (a renamed UI string broke `getByPlaceholder` in CI after three sound-but-partial sweeps), but narrower than the two above — it is Playwright-locator-shaped, and no second project here runs E2E specs today. Parked rather than filed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Next-up park                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-10 | 1 (G6)  | **WEEKLY close-out convention (`✅ PR #N`, never a bare ✅)**                                                                                                                                                                                                                                                                                                                    | G1–G4 close-outs                                                                                                                                                                                                                                                               | **pass**      | Already global — present in `~/.claude/CLAUDE.md`, `WORKFLOW.md` **and** `TEMPLATES/docs_template/planning/WEEKLY.md`. Both halves landed; two-trees holds. Checked rather than assumed, per the standing method.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-10 | 1 (G6)  | **Bidirectional docs-index check (rows ↔ headers + neighbours)**                                                                                                                                                                                                                                                                                                                 | G3 / G4 docs-freshness recurrences                                                                                                                                                                                                                                             | **propagate** | **Corrected from `pass` on PR #32 review.** The original verdict claimed the rule was "already present in `POLICIES/code-review.md`" — it is not. That claim came from a `grep -l` for the _concept_ (`bidirection\|both directions`), which matched line 279, `- [ ] Migration tested both directions`: a **database-migration** checklist item. The file's only docs line is a generic `- [ ] README updated if needed`, which is not this rule. Verified absent globally on re-check. It earns propagation on evidence — the drift class it prevents has been caught by human review on PRs #16, #17, #19, #21, #23 and #26 — and by Convention 9, since it currently lives only in per-project memory.                                                                                                                                          | `TODO.md` § 🔀 Spawned → Cross-project (row 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-15 | 2 (G10) | **Serverless freeze kills fire-and-forget side effects**                                                                                                                                                                                                                                                                                                                         | `serverless-freeze-kills-fire-and-forget` (memory, 2026-08-11) ← **PR #34** hotfix arc                                                                                                                                                                                         | **propagate** | The window's strongest candidate and the only one backed by a **production incident**: `create-order` called `sendOrderConfirmationEmail(...).catch(() => {})` without `await`, the Vercel instance froze at response-return, and prod sent **zero** order emails while returning 200 — invisible to local dev (long-running server) and to CI. Verified absent from the global tree: greps for `fire.and.forget\|unawaited\|serverless freeze\|await.*before.*respond` across `~/.claude` (excluding `projects/` and `plugins/`) return **no match**. Universal to any serverless target — Vercel, Lambda, Cloudflare Workers — and the failure is silent by construction. Carries its own regression shape: a race test asserting the response promise cannot resolve before the side-effect promise. Natural home: `POLICIES/error-handling.md`. | `TODO.md` § 🔀 Spawned → Cross-project (row 4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-15 | 2 (G10) | **The working copy moves mid-session — re-read live git state before any close-out**                                                                                                                                                                                                                                                                                             | `working-copy-moves-mid-session` (memory, 2026-08-15) ← **PR #38** arc                                                                                                                                                                                                         | **propagate** | Not project-shaped at all: it is a property of how this user works — editing the _same checkout_ while a session runs. On 2026-08-15 every field of the session-start snapshot went false during an ~8-minute agent fan-out (branch, HEAD, PR state), and a doc write failed on a path that close-out had already archived. Verified absent globally: `git status.*before\|re-read.*git\|stale.*git status\|working copy.*change` → **no match**. Applies to every project this user runs an agent in, and the mitigation is three commands. Natural home: `WORKFLOW.md` (close-out steps) or `POLICIES/documentation.md`.                                                                                                                                                                                                                          | `TODO.md` § 🔀 Spawned → Cross-project (row 5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-15 | 2 (G10) | **Diff review must read in-branch history before treating a frozen plan as authority** (the half that is missing)                                                                                                                                                                                                                                                                | `in-branch-decisions-invisible-to-diff-review` (memory, 2026-08-11) ← **PR #33** / G5 arc                                                                                                                                                                                      | **propagate** | Convention 10 applied honestly: this one is **partly** covered, and the covered part is quoted rather than assumed. `~/.claude/CLAUDE.md:63` already says _"Frozen plans/specs get a superseded note; a live doc must simply be **corrected**."_ — that is the authoring half. The **review** half is absent: a reviewer reading a diff against a frozen plan reports a _deliberate in-branch reversal_ as a regression, and the fix is to run `git log main..HEAD` first. Its corollary is absent too — multiple agents agreeing on the same truncated input is not corroboration. Only the missing half propagates; the covered half explicitly does not. Natural home: `POLICIES/code-review.md`.                                                                                                                                                | `TODO.md` § 🔀 Spawned → Cross-project (row 6)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-15 | 2 (G10) | **Claimed records must exist — make the durable write before claiming it**                                                                                                                                                                                                                                                                                                       | `claimed-records-must-exist` (memory, 2026-08-15) ← **PR #37** / G9 arc                                                                                                                                                                                                        | **defer**     | Genuinely universal and verified absent (`before claiming\|claim.*recorded\|record.*before.*claim` → **no match**). Deferred for a **routing** reason, not a value one: it is the same paragraph as run 1's still-open propagation _"never write execution records ahead of execution"_, in the same target file, and splitting one rule across two out-of-tree errands filed a week apart makes both likelier to rot. **Re-trigger**: run 1's row 2 gets actioned — fold this in as its second sentence at that moment.                                                                                                                                                                                                                                                                                                                            | Next-up park (fold into run 1 row 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-15 | 2 (G10) | **Gate screenshots must go through an Artifact URL**                                                                                                                                                                                                                                                                                                                             | `gate-screenshots-via-artifact` (memory, 2026-08-14) ← **PR #35** / G8 gate                                                                                                                                                                                                    | **defer**     | Verified absent (`screenshot.*(share\|artifact\|url)\|inline image\|artifact url` → **no match**), and real — chat-inline images never reach the user, so six visual-gate rounds could have run blind. But it is **harness-shaped** (Claude Code Artifacts), not project-shaped, and it only bites where a visual gate exists. **Re-trigger**: a second project runs a visual-fidelity gate — which is precisely run 1's row 1, still open. Fold in there rather than as its own errand.                                                                                                                                                                                                                                                                                                                                                            | Next-up park (fold into run 1 row 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-15 | 2 (G10) | **Code-review threshold understates doc findings** (18 recurrences)                                                                                                                                                                                                                                                                                                              | `code-review-threshold-understates-doc-findings` (memory, updated 2026-08-15) ← **PRs #37, #38** reviews                                                                                                                                                                       | **defer**     | The best-evidenced rule in the whole window — 18 recurrences, the user fixes nearly all sub-threshold findings — and verified absent (`severity (rubric\|threshold)\|sub-threshold\|below the (gate\|threshold)` → **no match**). It defers because it has **no reachable sink**: the rubric it describes lives in the `code-review` **plugin**, which is third-party and not editable from `~/.claude`. Writing it into the user's config would be advice about someone else's tool rather than a rule the user's own workflow executes. **Re-trigger**: the plugin exposes a configurable threshold, **or** the local BACKLOG's existing "lower/reword the severity rubric" option is worked — at which point the finding has a real home.                                                                                                        | Next-up park                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-15 | 2 (G10) | **Backlog reaps: move to a 🪦 section, never delete**                                                                                                                                                                                                                                                                                                                            | `backlog-reaps-move-not-delete` (memory, 2026-08-11) ← weekly-planning user ruling                                                                                                                                                                                             | **defer**     | A **user preference**, not a discovered failure mode — ruled 2026-08-11 and already executed here with 5 tombstone rows. Verified absent from the global tree (the only `reap\|tombstone\|🪦` hits are in `cache/changelog.md`, a third-party cache the corollary excludes by design). Its natural home is `TEMPLATES/docs_template/planning/BACKLOG.md`, so future projects inherit the convention. Deferred as a **fold-in** rather than an errand: a one-line template edit is not worth a standalone out-of-tree task while three propagations sit unactioned. **Re-trigger**: that template is edited for any reason.                                                                                                                                                                                                                          | Next-up park (fold into template edit)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-15 | 2 (G10) | **Stack-specific technical facts (4, scanned as one group)** — Tailwind v4 dropping `@media` inside `@layer utilities` in prod builds; `next dev` serving a stale `.next` cache; the CSS-verification/minifier gotchas; hydration null-first-render breaking mount effects                                                                                                       | Memory files dated 2026-08-11 / 2026-08-14 ← **PRs #35, #36**                                                                                                                                                                                                                  | **pass**      | Scanned and recorded per Convention 1, grouped per this run's declared candidate rule — a candidate is a _durable process rule_, and these four are version-pinned facts about Tailwind v4 / Next 14, not rules. Each is genuinely load-bearing **here** (the Tailwind one caused a production CSS defect that a 5-agent review cleared on a vacuous test), but propagating them to a global tree shared with non-Next projects would put stack trivia in a policy file, and they are already captured where they are actionable — this project's memory and `CLAUDE.md`'s Git Insights. The one _transferable_ shape inside them — "verify the compiled/production output, never the dev server" — is already implied by run 1's propagated visual-fidelity gate.                                                                                  | — (row only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-09-12 | 3 (G21) | **Verify the rejection, not the render** — after fixing an allowlist or guard, probe the inputs that must be _refused_; a working happy path proves only that the guard did not break the feature                                                                                                                                                                                | `verify-the-rejection-not-the-render` (memory, 2026-09-04) ← **PR #43** / G17 finding F6                                                                                                                                                                                       | `propagate`   | Verified absent. `POLICIES/security.md` carries the allowlist only as an authoring requirement — lines 124–125 read \_"Emails                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Format, domain allowlist if applicable"_ and _"URLs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Protocol (https only), domain allowlist"\_ — which is the rule for writing the guard, not for verifying one. Nothing in the global tree says the verification of a guard fix is its rejections. The evidence is first-hand and recent: F6 (the image-optimizer SSRF) was only _proved_ closed when arbitrary, cloud-metadata and lookalike hosts all returned 400 in production; that real R2 images still rendered was consistent with the guard having done nothing at all. Universal — every allowlist, every auth check, every input filter has the same asymmetry. | `TODO.md` § 🔀 Spawned → `POLICIES/security.md` |
| 2026-09-12 | 3 (G21) | **A guard must be shown to fail before it is trusted — and `expect(x.length > 0)` is not that proof**: compare against an independently computed count                                                                                                                                                                                                                           | `guards-need-teeth-and-token-layer-coverage` (memory, 2026-09-10) ← **PR #45** / G19, where the same defect appeared **5×** in one PR                                                                                                                                          | `propagate`   | Half already global, half absent — and the absent half is the actionable one. The principle is there twice: `CLAUDE.md:41` _"a check that cannot fail looks exactly like one that passes"_ and `POLICIES/critical-thinking.md:77` \_"Could this check have failed?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A guard that cannot fail looks identical to a guard that passes"\_, inside a whole Phase 4 built around it. What is missing is the technique, and `POLICIES/testing.md` has nothing: greps for `vacuous`, `toBeGreaterThan`, `independently` and `count` all return **zero**. So the global tree tells you to doubt a green check and never tells you how to build one that could go red. The specific anti-pattern is worth naming because it is invisible on review — `expect(set.length).toBeGreaterThan(0)` smuggles in the premise "this set is never legitimately empty", which is exactly the assumption under test. | `TODO.md` § 🔀 Spawned → `POLICIES/testing.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-09-12 | 3 (G21) | **A doc that quotes code shares no keyword with the change that invalidates it** — diff quoted snippets against source, and audit what a sweep _skips_                                                                                                                                                                                                                           | `quoted-code-drifts-invisibly-to-grep` (memory, 2026-09-10) ← **PR #45** / G19, which took 5 review rounds on exactly this                                                                                                                                                     | `propagate`   | Verified absent. The global tree covers the _other_ direction only: `CLAUDE.md:79` _"References deleted code → update or archive"_ and `TEMPLATES/docs_template/README.md:174` _"References deleted code/features"_ both assume the doc _names_ the thing that changed, which is what makes it findable. A quoted snippet is the case where it does not: rename the symbol and the doc keeps compiling in the reader head while sharing no token with the diff. Greps for `quoted code`, `code snippet` and `snippet … stale` return nothing. Natural home is `POLICIES/documentation.md`, and the mechanism generalizes to any repo whose docs embed code.                                                                                                                                                                                         | `TODO.md` § 🔀 Spawned → `POLICIES/documentation.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-12 | 3 (G21) | **Diff-scoped review is blind to the fallout of deletions** — what a removed symbol leaves behind is not in the diff                                                                                                                                                                                                                                                             | `diff-scoped-review-blind-to-deletion-fallout` (memory, 2026-08-21) ← **G12** merge `9fc4fd3`, a −576-line change                                                                                                                                                              | `defer`       | Verified absent **as a review rule**. The four matches are all a different rule — `WORKFLOW.md:435` \_"Code deletion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Remove obsolete scenarios"_ and `POLICIES/manual-testing.md:28` _"Code deletion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Remove scenarios that test deleted functionality"\_ are about pruning test scenarios, not about a reviewer reading a deletion diff. Deferred on **routing, not value**: its home is `POLICIES/code-review.md`, which is already the target of run 1 propagation row 3 and still unchecked. **Re-trigger**: that row is actioned — fold this in as its second paragraph rather than opening a second errand against the same file.                                                                                                                                       | Next-up, fold-in against run 1 row 3            |
| 2026-09-12 | 3 (G21) | **An unverified reproduction premise can invert the fix** — confirm the bug reproduces the way you think before choosing a remedy                                                                                                                                                                                                                                                | `unverified-repro-premise-inverts-the-fix` (memory, 2026-08-17) ← the **G11** merge `745e039`                                                                                                                                                                                  | `defer`       | Partially covered, and the covering text is strong: `POLICIES/critical-thinking.md` Phase 4 asks _"Was this measured, or asserted from a plausible model?"_ and _"Did I design this probe to characterize, or to confirm?"_ — a reader applying Phase 4 to a bug report arrives at this rule. What Phase 4 does not say is that the premise most often skipped is the _reproduction_ itself, where being wrong does not merely weaken the fix but points it the wrong way. One sentence in an existing section, not a new rule. **Re-trigger**: Phase 4 is edited for any reason.                                                                                                                                                                                                                                                                   | Next-up, fold-in to `POLICIES/critical-thinking.md` Phase 4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-09-12 | 3 (G21) | **A guard that reads a directory breaks when the directory empties** — git does not track empty directories, so the last file moved out turns a passing check into an ENOENT                                                                                                                                                                                                     | `close-out-archive-breaks-directory-guards` (memory, 2026-09-12) ← **G19 close-out**, which broke `main`                                                                                                                                                                       | `defer`       | Verified absent (greps for `empty director`, `git does not track`, `ENOENT` → **zero**). Real and nastily timed — it fires during a close-out, after the work is approved, on a branch nobody is watching. Deferred because it is the same paragraph as the guard rule propagating above: a directory-reading guard needs a case for "the directory is gone", which is precisely the "could this check have failed" family. **Re-trigger**: the `POLICIES/testing.md` propagation lands — fold this in there as the worked example.                                                                                                                                                                                                                                                                                                                 | Next-up, fold-in against this run propagation row 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-12 | 3 (G21) | **Real-data intake finds what review cannot — and a green test can be vacuous through its fixture**                                                                                                                                                                                                                                                                              | `real-data-intake-finds-what-review-cannot` (memory, 2026-09-10) ← **PR #41** / G16, where 5 review agents found neither of 2 runtime bugs that an hour of real data surfaced                                                                                                  | `defer`       | Verified absent (no `fixture`, `real data` or seed-vs-real matches anywhere in the global tree). Two distinct rules live in this memory, and only the second is fully portable: that a passing test can be vacuous **because its fixture cannot express the failing case**, which is the fixture-side twin of the assertion-side rule propagating above. The first — that writing the user-facing guide is itself a review technique — is real but reads as advice rather than a check. Deferred on routing: its home is `POLICIES/manual-testing.md`, the target of run 1 propagation row 1, still unchecked. **Re-trigger**: row 1 is actioned; fold in there.                                                                                                                                                                                    | Next-up, fold-in against run 1 row 1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-12 | 3 (G21) | **Underspecified predicates manufacture disagreement** — two reviewers arguing about a rule usually disagree about its wording, not the code                                                                                                                                                                                                                                     | `underspecified-predicates-manufacture-disagreement` (memory, 2026-08-18) ← **G12**                                                                                                                                                                                            | `defer`       | Verified absent. Genuinely portable — it applies to any rule read by more than one party, human or agent — but it is a rule _about writing rules_, and the global tree has no section that owns that; dropping it into `POLICIES/code-review.md` would make it a stray. **Re-trigger**: the judgement-over-rules rewrite of the instruction files runs (already parked from run 2 for the Cleanup Week) — that edit is exactly where a rule about rule wording belongs. Fold in there.                                                                                                                                                                                                                                                                                                                                                              | Next-up, fold-in to the judgement-over-rules park                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-09-12 | 3 (G21) | **Stack-specific technical facts (6, scanned as one group)** — GFM table pipes unprotected by code spans; the CSS-verification and Next minifier gotchas; a Server Component `redirect()` is not a 3xx; Vitest `once` queues leaking across tests; a duplicate `DATABASE_URL` in `.env` silently winning; Tailwind v4 dropping `@media` inside `@layer utilities` in prod builds | Memory files dated 2026-08-17 → 2026-09-12 ← the **G11** merge `745e039` (GFM pipes), the **G12** merge `9fc4fd3` (CSS verification, Server Component `redirect()`), **PRs #41/#43** (duplicate `DATABASE_URL`), **PR #44** (Vitest `once`) and **PR #46** (Tailwind `@layer`) | `defer`       | Grouped per the run 2 rule — Convention 1 wants a row per candidate _considered_, not per file read. None is a process rule; each is a fact about a tool this project happens to use, and their value out-of-tree is exactly proportional to another project sharing the tool. Two are closer to portable than the rest: the GFM pipe-escaping fact applies to anyone authoring Markdown tables, and the Vitest `once`-queue leak to any Vitest suite. **Re-trigger**: a second project adopts this stack, at which point `LANGUAGES/typescript.md` and `LANGUAGES/javascript.md` are the hosts — not before, since a fact with no local reader is noise in a global file.                                                                                                                                                                          | Next-up, one park for the group                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-09-12 | 3 (G21) | **"Claimed records must exist" — its 2026-09-02 update adds that propagation greps must ENUMERATE their targets**                                                                                                                                                                                                                                                                | `claimed-records-must-exist` (memory, updated 2026-09-02; run 2 deferred the original) ← scope has failed **3×**, latest the path-scoped module docs                                                                                                                           | `defer`       | Still the same routing answer as run 2 — it is one paragraph with run 1 propagation row 2, in the same target file — but the update deserves recording because it is a rule about **this slot**: a propagation check that greps without first enumerating its targets reports success from an incomplete scan, and that has now failed three times. This run took the instruction literally, enumerating the global tree with `find` before grepping and excluding `projects/` and `plugins/` per the Convention 9 corollary. **Re-trigger**: unchanged — run 1 row 2 is actioned, fold in as its second sentence.                                                                                                                                                                                                                                  | Next-up, fold-in against run 1 row 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-12 | 3 (G21) | **The `/code-review` run has a cost worth budgeting for**                                                                                                                                                                                                                                                                                                                        | `code-review-skill-cost-2026-09` (memory, 2026-09-10) ← the **G18** review incident                                                                                                                                                                                            | `pass`        | Verified absent from the global tree (no `token cost`, `cost ceiling` or `expensive review` matches), and correctly so. It is a fact about one third-party plugin at one point in time, and the durable half is already where it belongs: a standing user preference to ask before launching any multi-agent review, and never to re-launch one after a session-limit kill. A cost figure in a global policy file would be stale within a release, and the preference already prevents the failure. Row only.                                                                                                                                                                                                                                                                                                                                       | Row only — no sink                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

**Run 2 scan window (declared before scanning, 2026-08-15)**: everything shipped here since run 1's
merge `8298dab` — **PRs #33, #34, #35, #36, #37, #38** and their close-outs, plus every auto-memory
file created or materially updated **2026-08-11 → 2026-08-15**. This is wider than the window WEEKLY
named when the plan was written on 2026-08-11 ("PR #33 close-out, the PR #34 hotfix arc, the G7
deferral, new memory files") — PRs #35–#38 had not shipped yet. The slot's own definition ("shipped
since run 1") governs; widened by user ruling at the G10 brainstorm. **Candidate rule**: a candidate
is a _durable process rule that could apply outside this repo_. Stack-specific technical facts are
scanned too, but recorded as one grouped row rather than one row each — Convention 1 requires a row
per candidate considered, not per file read.

---

## Next-up

Parked runners-up and `defer` verdicts. Each entry carries a **re-trigger condition** — check the condition
cheaply, and if it is unmet, move on without re-reading the source (Convention 4).

### From run 1 (2026-08-10, G6)

- **`resend` — Resend agent skills + MCP** (slot 1, official) — `defer`.
  **Re-trigger**: email work goes beyond copy/styling — i.e. a `react-email` migration of the
  hand-rolled HTML-string templates is scoped, **or** the real sending domain is provisioned
  (TASK-056 client round-trip) and deliverability/DNS-authentication work becomes actionable.
  Until then the cheap check is the existing [2026-08-07] 🔵 "Verify prod email config" entry —
  do not re-read the skill source (Convention 4).
- **`typescript-lsp`** (slot 1 runner-up, official) — parked unreviewed.
  **Measured**: the `typescript-language-server` binary is **not installed** in this container
  (only `tsserver`, which is the compiler's server, not the LSP wrapper), so adopting it costs a
  global npm install. It is the TypeScript analogue of the `pyright-lsp` adopt in the sibling
  Python repos. **Re-trigger**: type-navigation or rename friction shows up in practice, or the
  binary lands in the toolchain for another reason.
- ~~**`claude-security` / `security-guidance`** (slot 1 runners-up, official) — parked unreviewed.
  Both are first-party. Noted here because this repo, unlike the sibling that passed
  `security-guidance` for having no app-code web surface, **is** a Next.js app with auth, API
  routes and order-creation code. **Re-trigger**: the pre-launch security pass is scheduled, or
  real customer traffic is imminent (which is also the pin on the standing G2 hardening bundle).~~
  **CONDITION FIRED 2026-08-15 (run 2)** — the pre-launch week is user-confirmed for Aug 17–21.
  Both were reviewed rather than re-parked wholesale: `claude-security` → **adopt**,
  `security-guidance` → **defer** under a narrower condition (see run 2 below). This park is closed.
- **Slot 3 category-bias watch** (slot 3, process) — not a candidate; a counter.
  Slot 3 passed on run 1 for the documented structural reason: a **single-tool, solo, no-runtime-LLM**
  project cannot adopt cross-tool portability practices. Sibling projects hit the same wall
  repeatedly and eventually filed a reframe request. One local data point is not a pattern, so
  nothing is filed yet. **Re-trigger**: if slot 3 passes for this same reason in **two more runs
  here** (i.e. runs 2 and 3), file a 🟤 to re-scope the category — or pause it and redistribute its
  1 SP — rather than keep spending the slot to reach a foregone conclusion.
  **Run 2 (2026-08-15): the counter did not advance, and the condition it counted against was
  retired.** Run 2's slot 3 also passed, but for a different reason (candidate already practiced
  under another name), and it still produced a live design input for a group scheduled the same
  week. The diagnosis in the original park was therefore too broad: the dead end is not the
  _category_, it is **searching the category for portability practices**, which a single-tool
  project can never use. **Condition rewritten**: file the 🟤 only if slot 3 yields nothing usable
  in two more runs _when the search is aimed at tool-agnostic methodology_ (SDD, evals,
  verification, review technique) rather than at cross-tool portability. If the next run's slot 3
  again reaches for `AGENTS.md`-shaped material, that is the search to fix, not the slot to pause.
  **Counter re-bases to 0 of 2** — not "1 of 2". Rewriting a condition re-bases what it counts:
  run 1's pass was portability-aimed and does not qualify under the new rule, and run 2 yielded a
  usable finding, so no run yet counts toward it. _(Caught 2026-08-15 by the strengthened check 2
  while fixing the PR #39 finding — the old "1 of 2" was measuring against a rule that had just
  been retired. A counter carried across a condition rewrite is stale by construction.)_
- **`AGENTS.md` / cross-tool shared-context instruction files** (slot 3) — parked, condition unmet.
  **Re-trigger**: a second agentic tool (Codex, Cursor, Copilot coding agent) actually enters this
  project's workflow. Until then the cheap check is "is there a second tool?" — do not re-read the
  sources (Convention 4). Note the mechanism if it ever triggers: Claude Code reads `CLAUDE.md`, not
  `AGENTS.md`, and bridges via an `@AGENTS.md` import or a symlink.
- **"String renames must sweep every locator type"** (slot 4) — `defer`, not filed outward.
  Evidence-backed here (a renamed UI string broke `getByPlaceholder` in CI after three
  sound-but-partial sweeps, PR #31), but Playwright-locator-shaped and narrower than the two rows
  that did propagate. **Re-trigger**: a second project with an E2E suite enters the picture, or
  `~/.claude/POLICIES/testing.md` is being edited anyway — fold it in then rather than as its own
  errand.

#### Run 1 park re-checks performed on 2026-08-15 (run 2, Convention 4 — cheap checks only)

| Park                                    | Condition                                                        | Cheap check run                                                                                                                             | Outcome                                    |
| --------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| `resend`                                | `react-email` migration scoped **or** sending domain provisioned | `grep -c react-email package.json` → **0**; TASK-056 still ⏸️ deferred, prod sender still the interim `onboarding@resend.dev`               | **unmet** — re-parked, source not re-read  |
| `typescript-lsp`                        | Type-nav/rename friction, or the binary lands anyway             | `command -v typescript-language-server` → **NOT INSTALLED** (unchanged since run 1); no rename friction raised in G9/G14, both string-heavy | **unmet** — re-parked, source not re-read  |
| `claude-security` / `security-guidance` | Pre-launch security pass scheduled, or traffic imminent          | Pre-launch week user-confirmed **Aug 17–21**                                                                                                | **FIRED** — both reviewed this run         |
| `AGENTS.md` / cross-tool                | A second agentic tool enters the workflow                        | `find` for `AGENTS.md` / `.cursorrules` / `CODEX.md` → **none in tree**                                                                     | **unmet** — re-parked, sources not re-read |
| Slot 3 category-bias watch              | Slot 3 passes for the same structural reason in runs 2 and 3     | See slot 3 below — run 2's pass was for a **different** reason                                                                              | **did not advance; condition retired**     |
| String-rename locator sweep             | Second E2E project, or `POLICIES/testing.md` edited anyway       | No second project; no global-tree edit scheduled this run                                                                                   | **unmet** — re-parked                      |

### From run 2 (2026-08-15, G10)

- **`security-guidance`** (slot 1 runner-up, official) — `defer`, now **reviewed** (v2.0.7).
  Not a fit _this week_ because its cost is per-turn, not per-invocation: an LLM diff review on
  every Stop plus an agentic reviewer on every commit, defaulting to Opus. **Re-trigger**: the
  launch push is over and steady-state development resumes (per-turn cost becomes affordable),
  **or** `claude-security`'s pre-launch scan finds a vulnerability class that recurs in new code —
  which is the case continuous review answers and an on-demand scan does not. If it triggers, note
  the cheap knobs already read from its README: `ENABLE_STOP_REVIEW=0` keeps commit review without
  the per-turn tax, and `SECURITY_REVIEW_MODEL=claude-sonnet-4-6` trades precision for cost.
- **`logic-lens`** (slot 1, internet) — `defer`. MIT, v0.6.10, 19 stars, real marketplace add.
  Aimed at this repo's most-recurrent review weakness (findings suppressed by a confidence
  threshold), but single-maintainer with a self-published benchmark, and a second reviewer costs
  review rounds the launch push cannot spare. **Re-trigger**: the launch push ends, **or** the
  `code-review` threshold suppresses a real finding once more (that would be the 19th recurrence)
  and a complementary trace-based reviewer becomes worth its rounds. Cheap check next run: has the
  repo grown past 19 stars / gained visible co-maintainers — do not re-read the skills (Convention 4).
- **Judgement-over-rules rewrite of the instruction files** (slot 2) — `defer`.
  The one part of the Claude-5 context-engineering post that no open item covers: converting
  fixed-count directives into judgement. **Re-trigger**: the Cleanup Week runs (user-confirmed
  for after site launch) — that is the slot this edit belongs in. Cheap check each run: has the
  Cleanup Week been scheduled? Do not re-read the post until then (Convention 4). When it does
  run, the BACKLOG entry already carries the worked example and the second axis.
- **Convention 7 re-trigger — partially fired (slot 2, process).** Convention 7 parks the
  three-scope lens until "a second active project starts consuming this repo's conventions, **or a
  run produces a candidate with an obvious non-local fit and no sink for it**." Slot 2 is that
  candidate: its sharpest target is the **global** `~/.claude/CLAUDE.md`, and this repo's sinks
  (BACKLOG 🟤, TODO 🔀) are for in-tree work and out-of-tree _propagation of learnings_ — neither
  is a home for "rewrite the global config's style". The half that _does_ have a local sink (this
  repo's own 350-line `CLAUDE.md`) was routed there. Recorded, not acted on: one occurrence is
  not yet the pattern Convention 7 asks for, and the slot-4 sink partly absorbed it this run.
  **Re-trigger**: a second such candidate appears, at which point un-defer the three-scope lens
  rather than keep improvising a sink.
- **"Claimed records must exist"** (slot 4) — `defer`, verified absent globally.
  Deferred on **routing**, not value: it is the same paragraph as run 1's still-open
  propagation "never write execution records ahead of execution", in the same target file.
  **Re-trigger**: run 1's row 2 gets actioned — fold this in as its second sentence then.
- **"Gate screenshots via an Artifact URL"** (slot 4) — `defer`, verified absent globally.
  Real (chat-inline images never reach the user) but harness-shaped, and it only bites where a
  visual gate exists. **Re-trigger**: a second project runs a visual-fidelity gate — i.e. run 1's
  row 1 being actioned. Fold in there.
- **"Code-review threshold understates doc findings"** (slot 4) — `defer`, verified absent globally.
  The best-evidenced rule in the window (18 recurrences) and the one with **no reachable sink**:
  the rubric lives in the third-party `code-review` plugin, not in `~/.claude`. **Re-trigger**:
  the plugin exposes a configurable threshold, or the local BACKLOG's "lower/reword the severity
  rubric" option is worked.
- **"Backlog reaps: move to 🪦, never delete"** (slot 4) — `defer`, verified absent globally.
  A user preference rather than a discovered failure mode; natural home is
  `TEMPLATES/docs_template/planning/BACKLOG.md`. **Re-trigger**: that template is edited for any
  reason — a one-line fold-in, not a standalone errand.

**Standing note on the propagation queue (observed 2026-08-15, not a verdict).** All **3** of run
1's propagation rows are still unchecked in `TODO.md`, and run 2 adds 3 more. That is by design —
the sink is user-maintained because this repo cannot verify out-of-tree completion — but it is why
four of this run's slot-4 candidates were routed as **fold-ins** against existing rows rather than
as new errands. If the queue is worked as a batch, work the folds at the same time.

### From run 3 (2026-09-12, G21)

- **`logic-lens`** (slot 1, internet) — `defer` a second time; condition **narrowed**.
  Nothing about the candidate changed since run 2 (v0.6.10, 22 stars, single maintainer,
  self-published benchmark), so re-reading its skills would have been the Convention 4 mistake in
  reverse: the condition fired, but on the evidence about _our_ reviewer, not about this tool.
  **Re-trigger rewritten**: the 🟤 rubric entry is worked and the quantization is still unfixed
  afterwards — i.e. the gate demonstrably cannot be made to admit this finding class — **or**
  logic-lens gains a co-maintainer or an independent benchmark. Recurrence count alone no longer
  re-triggers it: 24 recurrences did not make a second reviewer the right instrument, and 25 will
  not either. Cheap check next run: `git ls-remote` / the repo header for version and maintainers —
  do not re-read the skills.
- **`neon`** (slot 1, official) — `defer`, v1.2.0, Apache-2.0, first-party.
  **Re-trigger**: a Neon credential that can be scoped to **schema only** (no table data) exists,
  **or** a non-PII staging/preview Neon branch enters the workflow — either removes the PII
  exposure that is the actual objection, not the vendor's dev-only advice. Cheap check next run:
  is there a second, data-free Neon branch or a schema-scoped key? Do not re-read the docs.
- **EARS acceptance-criteria notation** (slot 3) — `defer`.
  The gap is the point: it formalizes writing criteria, which this repo already derives from its own
  evidence, and is silent on proving a criterion can fail, which is where the recurrences are.
  **Re-trigger**: a spec is written whose acceptance criteria are disputed or read two ways during
  review (EARS would settle the wording), **or** the repo starts generating tests from spec criteria
  and needs a canonical shape for them. Cheap check next run: did any spec review this week turn on
  an ambiguous criterion? Do not re-read the Kiro docs.

- **`sentry` / `prisma`** (slot 1 runners-up, official) — parked unreviewed, sources not fetched.
  Recorded so the next run does not re-derive the shortlist. `sentry` (category `monitoring`,
  [getsentry/plugin-claude](https://github.com/getsentry/plugin-claude)) is the better _product_
  need — this repo has no error monitoring at all and `.env.example` still marks Sentry NOT YET
  IMPLEMENTED — but adopting the plugin buys nothing until the SDK is instrumented, which is a
  feature task, not a plugin decision. **Re-trigger**: Sentry instrumentation is scoped as work.
  `prisma` ([prisma/claude-plugin](https://github.com/prisma/claude-plugin)) is oriented at
  provisioning Prisma Postgres and managing connection strings, neither of which this repo needs —
  it runs Prisma against Neon and Docker. **Re-trigger**: a migration workflow problem that
  `prisma migrate` plus `scripts/vercel-build.sh` cannot answer.

- **Slot 4 fold-in defers (6, all verified absent globally)** — every one is deferred on
  **routing, not value**, and each names an existing unworked row as its host, per the standing note
  that this queue is worked as a batch: "diff-scoped review is blind to deletion fallout" → run 1
  row 3 (`POLICIES/code-review.md`); "an unverified reproduction premise inverts the fix" → the
  `critical-thinking.md` Phase 4 table, whenever it is next edited; "a directory-reading guard
  breaks when the directory empties" → this run's `POLICIES/testing.md` propagation, as its worked
  example; "real-data intake finds what review cannot" (fixture-side vacuity) → run 1 row 1
  (`POLICIES/manual-testing.md`); "underspecified predicates manufacture disagreement" → the
  judgement-over-rules rewrite parked from run 2, which is the one edit that owns rules about rule
  wording. The sixth is a **re-park rather than a new fold-in**: "claimed records must exist" was
  already folded against run 1 row 2 by run 2, and run 3 carries new content into the same host (its
  2026-09-02 update, that a propagation grep must enumerate its targets first). **Re-trigger for all
  six**: the named host is touched. Filing them as separate errands would open one edit per item
  across **six** host files — `POLICIES/code-review.md`, `critical-thinking.md`, `testing.md`,
  `manual-testing.md`, `documentation.md` and the instruction files — and **four** of those six
  already carry a queued propagation row (run 1's rows 1–3 plus this run's `testing.md` row), which
  is the whole reason to fold rather than file. The other two wait on an edit nobody has scheduled:
  Phase 4 "whenever it is next edited", and the judgement-over-rules rewrite at the Cleanup Week.
  That count includes the sixth, which contributes a host like the rest — what it spares, per the note
  above, is a new **fold** rather than a new file.
- **Stack-specific technical facts, 2026-08-17 → 2026-09-12** (slot 4, grouped) — `defer`.
  Six facts, no process rule among them (GFM table pipes; the CSS-verification and Next minifier
  gotchas; Server Component `redirect()` not being a 3xx; Vitest `once` queues leaking across
  tests; a duplicate `DATABASE_URL` winning silently; Tailwind v4 dropping `@media` inside
  `@layer utilities` in prod). **Re-trigger**: a second project adopts this stack — then
  `LANGUAGES/typescript.md` / `LANGUAGES/javascript.md` host them. The two with the widest reach if
  it ever fires are the GFM pipe-escaping rule and the Vitest `once`-queue leak.

#### Park re-checks performed on 2026-09-12 (run 3, Convention 4 — cheap checks only)

Every park from runs 1 and 2, checked against its own condition. Sources were **not** re-read where
the condition is unmet.

| Park                                                | From  | Condition                                                                             | Cheap check run                                                                                                                                                                                               | Outcome                                                              |
| --------------------------------------------------- | ----- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| `resend`                                            | run 1 | `react-email` migration scoped **or** sending domain provisioned                      | `grep -c react-email package.json` → **0**; TODO TASK-056 item 1 (🔴 Domain choice/purchase/DNS) still `- [ ]`, prod sender still the interim `onboarding@resend.dev`                                         | **unmet** — re-parked, source not re-read                            |
| `typescript-lsp`                                    | run 1 | Type-nav/rename friction, or the binary lands anyway                                  | `command -v typescript-language-server` → **NOT INSTALLED** (unchanged since runs 1–2); no rename friction raised in G15–G20                                                                                  | **unmet** — re-parked, source not re-read                            |
| `AGENTS.md` / cross-tool                            | run 1 | A second agentic tool enters the workflow                                             | `find` for `AGENTS.md` / `.cursorrules` / `CODEX.md` → **none in tree**                                                                                                                                       | **unmet** — re-parked, sources not re-read                           |
| String-rename locator sweep                         | run 1 | Second E2E project, or `POLICIES/testing.md` edited anyway                            | No second project; no global-tree edit scheduled this run                                                                                                                                                     | **unmet** — re-parked                                                |
| Slot 3 category-bias watch (counter **0 of 2**)     | run 1 | Slot 3 yields nothing usable when aimed at tool-agnostic methodology                  | Slot 3 searched methodology (EARS, a vendor-neutral requirements standard) and returned a design input now folded into the live 🟤 rubric entry                                                               | **did not advance — stays 0 of 2**, its third run without advancing  |
| `security-guidance`                                 | run 2 | Launch push over, **or** a vuln class recurs in new code                              | Convention-2 exclusion-set check: `security-guidance@claude-plugins-official` reads **`true` in the live `~/.claude/settings.json`** (v**2.0.8** now in the plugin cache, v2.0.7 also)                        | **CLOSED — the candidate is in use**; it left the pool by adoption   |
| `logic-lens`                                        | run 2 | Launch push ends, **or** the threshold suppresses a real finding once more (the 19th) | The threshold recurrence counter stands at **24** (memory `code-review-threshold-understates-doc-findings`, updated 2026-09-04 at G17 — 6×75 + 1×65 against an 80 gate, all seven real)                       | **FIRED** — reviewed this run, slot 1                                |
| Judgement-over-rules rewrite                        | run 2 | The Cleanup Week runs                                                                 | Cleanup Week still user-pinned to **after site launch**; the site has not launched                                                                                                                            | **unmet** — re-parked, post not re-read                              |
| Convention 7 / three-scope lens                     | run 2 | A second candidate with obvious non-local fit and no sink appears                     | Every candidate this run had a reachable sink: slot 2 targets this file own Convention 8 (🟤), the three slot 4 propagations target `TODO.md` § 🔀, and the threshold finding folds into an existing 🟤 entry | **did not fire** — the lens stays deferred, still on one occurrence  |
| "Claimed records must exist" (fold-in)              | run 2 | Run 1's propagation row 2 gets actioned                                               | TODO § 🔀 row 2 ("never write execution records ahead of execution") still `- [ ]`; all **6** propagation rows remain unchecked                                                                               | **unmet** — re-parked as a fold-in                                   |
| "Gate screenshots via an Artifact URL" (fold-in)    | run 2 | A second project runs a visual-fidelity gate (row 1 actioned)                         | TODO § 🔀 row 1 still `- [ ]`                                                                                                                                                                                 | **unmet** — re-parked as a fold-in                                   |
| "Code-review threshold understates doc findings"    | run 2 | The plugin exposes a configurable threshold, **or** the local rubric option is worked | Plugin README § "Adjusting confidence threshold": _"The default threshold is 80. To adjust, modify the command file at `commands/code-review.md`"_ … _"Change `80` to your preferred threshold (0-100)"_      | **FIRED (mechanism exposed, but see slot 4)** — re-reviewed this run |
| "Backlog reaps: move to 🪦, never delete" (fold-in) | run 2 | `TEMPLATES/docs_template/planning/BACKLOG.md` is edited anyway                        | That template is dated **2026-05-31** and contains no `🪦` section; no edit scheduled                                                                                                                         | **unmet** — re-parked as a fold-in                                   |
